{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"echo '=== DEBUG ===' && echo 'DATABASE_URL='$DATABASE_URL && echo 'SECRET_KEY='$SECRET_KEY && echo 'ENVIRONMENT='$ENVIRONMENT && echo 'BACKEND_CORS_ORIGINS='$BACKEND_CORS_ORIGINS && echo '============' && uvicorn app.main:app --host 0.0.0.0 --port $PORT\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
